name: Build legate.core
on: [push, pull_request]
jobs:
  build:
    runs-on: self-hosted
    steps: Run CI
      - name: Run CI
        run: |
          cd  /data/github-runner/_work/legate.ci
          git pull
