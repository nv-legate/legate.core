#!/usr/bin/env bash

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
="$1"
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
NAME="$2"
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
orary file to store the multipart form data
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
=$(mktemp)
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
ltipart form data
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
ID"
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
nt-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
nt-Type: application/zip"
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
PATH"
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
ID--"
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
"
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
le as an artifact
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
 application/vnd.github.v3+json" \
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
zation: Bearer $GITHUB_TOKEN" \
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
-Type: multipart/form-data; boundary=$UUID" \
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
ry "@$TEMP_FILE" \
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
loads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}
mporary file
upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}

upload_artifact() {
    # Variables
    local FILE_PATH="$1"
    local ARTIFACT_NAME="$2"
    local BOUNDARY=$(uuidgen)

    # Create a temporary file to store the multipart form data
    local TEMP_FILE=$(mktemp)

    # Create the multipart form data
    {
        echo "--$BOUNDARY"
        echo "Content-Disposition: form-data; name=\"file\"; filename=\"$(basename $FILE_PATH)\""
        echo "Content-Type: application/zip"
        echo
        cat "$FILE_PATH"
        echo "--$BOUNDARY--"
    } > "$TEMP_FILE"

    # Upload the file as an artifact
    curl -X POST \
        -H "Accept: application/vnd.github.v3+json" \
        -H "Authorization: Bearer $GITHUB_TOKEN" \
        -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
        --data-binary "@$TEMP_FILE" \
        "https://uploads.github.com/repos/$REPO_OWNER/$REPO_NAME/actions/artifacts/$ARTIFACT_NAME"

    # Remove the temporary file
    rm "$TEMP_FILE"
}


upload_output() {
    set -xeuo pipefail

    local artifact_name="$1"

    tar -czvf "${artifact_name}.tar.gz" /tmp/out /tmp/conda-build

    upload_artifact "${artifact_name}.tar.gz" "$artifact_name"
}

(upload_output "$@");