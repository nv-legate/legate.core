#!/usr/bin/env bash

build_all() {
    cd ~/;

    if type conda >/dev/null 2>&1; then
        . make-conda-env;
    fi
    
    set -xeuo pipefail;
    
    build-legate-cpp;
    build-legate-wheel;
    build-legate-conda;
}

(build_all "$@");
