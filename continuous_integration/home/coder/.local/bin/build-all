#!/usr/bin/env bash

build_all() {
    cd ~/;

    . activate-conda-env;
    
    conda info

    set -xeuo pipefail;
    
    build-legate-cpp;
    build-legate-wheel;
    build-legate-conda;
}

(build_all "$@");
